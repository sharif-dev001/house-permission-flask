services:
  - type: web
    name: house-permission-system
    env: python
    plan: free
    buildCommand: cd house_permission_system && pip install -r requirements.txt
    startCommand: cd house_permission_system && gunicorn app:app